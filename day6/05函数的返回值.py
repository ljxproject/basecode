# 需求: 定义一个函数可以求两个数的和, 并将该结果返回
def addNumber(a,b):
    # 功能代码
    count = a + b
    # 1.return可以返回一个结果,调用者执行完后的结果就是该返回值
    # 2.return可以省略, 省略后默认返回None,调用者执行完后的结果是None(注意 )
    # 3.return  a + b (return  表达式), 是将表达式的结果返回,(先执行表达式,再返回)
    # return count

    # return可以用来结束代码的执行,执行return后该函数执行结束,return后面的代码不会再执行
    return
    print("8888888888888")

# 调用
# addNumber(10,10)
count = addNumber(10,10)
# 不能使用None参数
# addNumber(count,10)
print(count)





















